
#include "Animal.h"

/*
void AnimalUpdateWorldHook( Leadwerks::Entity* entity );
void AnimalUpdatePhysicsHook( Leadwerks::Entity* entity );
void AnimalCollisionHook( Leadwerks::Entity* entity, const Leadwerks::Vec3& position, const Leadwerks::Vec3& normal, const float speed );

cAnimal::cAnimal(  Leadwerks::Vec3 Position, Leadwerks::Vec3 Direction, Leadwerks::Vec3 Scale ) {
	L_entity->AddHook( Leadwerks::Entity::UpdateWorldHook,		AnimalUpdateWorldHook );
	L_entity->AddHook( Leadwerks::Entity::UpdatePhysicsHook,	AnimalUpdatePhysicsHook );
	L_entity->AddHook( Leadwerks::Entity::CollisionHook,		AnimalCollisionHook );
}

cAnimal::~cAnimal() {  }

void cAnimal::UpdateWorld() {
	
}

void cAnimal::UpdatePhysics() {
	L_entity->UpdatePhysics();
}

void cAnimal::OnCollision( Leadwerks::Entity* entity2, const Leadwerks::Vec3& position, const Leadwerks::Vec3& normal, const float speed ) {
	L_entity->OnCollision( entity2, position, normal, speed );
}



void AnimalUpdateWorldHook( Leadwerks::Entity* entity ) {
//	if ( GetActor() ) Actor().UpdateWorld();
}

void AnimalUpdatePhysicsHook( Leadwerks::Entity* entity ) {
//	if ( GetActor() ) Actor().UpdatePhysics();
}

void AnimalCollisionHook( Leadwerks::Entity* entity, const Leadwerks::Vec3& position, const Leadwerks::Vec3& normal, const float speed ) {
//	if ( GetActor() ) Actor().OnCollision( entity, position, normal, speed );
}*/